# ERA5 Download Pipeline Requirements

# Core dependencies
cdsapi>=0.5.1          # Copernicus Climate Data Store API
xarray>=2023.1.0       # NetCDF data handling
netCDF4>=1.6.0         # NetCDF file support
numpy>=1.21.0          # Numerical operations
pandas>=1.5.0          # Data manipulation and timestamps

# Utilities
tqdm>=4.65.0           # Progress bars
psutil>=5.9.0          # System resource monitoring
requests>=2.28.0       # HTTP requests (dependency of cdsapi)

# Optional but recommended
dask>=2023.1.0         # Parallel computing for large datasets
matplotlib>=3.5.0      # Data visualization (for future analysis)
cartopy>=0.21.0        # Geospatial plotting (optional)

# Machine Learning Dependencies
scikit-learn>=1.3.0    # Core ML algorithms (Linear, SVR, RF)
lightgbm>=4.0.0        # LightGBM with GPU support
xgboost>=1.7.0         # XGBoost with GPU support  
catboost>=1.2.0        # CatBoost with GPU support
shap>=0.42.0           # Model interpretability and SHAP analysis
optuna>=3.3.0          # Hyperparameter optimization
joblib>=1.3.0          # Parallel computing and model persistence
seaborn>=0.12.0        # Enhanced statistical visualization
plotly>=5.15.0         # Interactive visualizations for SHAP plots